spring:
  sql:
    init:
      mode: always
      data-locations: classpath:static/data/data.sql
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true

server:
  servlet:
    encoding:
      force-response: true